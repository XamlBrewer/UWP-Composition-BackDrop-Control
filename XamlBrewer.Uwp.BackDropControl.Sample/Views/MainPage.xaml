<Page x:Class="XamlBrewer.Uwp.BackDropControl.Sample.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:XamlBrewer.Uwp.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Image Grid.Row="1"
               Grid.ColumnSpan="3"
               Stretch="Uniform"
               Source="ms-appx:///Assets/butterflies.png" />

        <controls:BackDrop BlurAmount="{Binding ElementName=Slider, Path=Value, Mode=TwoWay}"
                           TintColor="BlueViolet"
                           Grid.Row="1"
                           Grid.Column="1" />
        <Slider x:Name="Slider"
                Value="10"
                Grid.Row="2"
                Grid.Column="1" />
    </Grid>
</Page>
